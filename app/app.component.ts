import { Component } from '@angular/core';
import { Animal } from './animal.model';

@Component({
  selector: 'app-root',
  template: `
  <div class="jumbotron">
    <div class="container">
      <h1>Pandamonium</h1>
    </div>
  </div>
  <div class="container">
    <h2>This is just a filler screenshot for now. Check back later for finished look.</h2>
  </div>
  `
})

export class AppComponent {
}
